<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE module PUBLIC
    "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
    "https://checkstyle.org/dtds/configuration_1_3.dtd">

<module name="Checker">
    <module name="TreeWalker">

        <!-- Nome das classes deve começar com letra maiúscula -->
        <module name="TypeName"/>

        <!-- Nome das variáveis deve ser em camelCase -->
        <module name="LocalVariableName"/>

        <!-- Nome dos métodos deve ser em camelCase -->
        <module name="MethodName"/>

        <!-- Proíbe linhas muito longas -->
        <module name="LineLength">
            <property name="max" value="120"/>
        </module>

        <!-- Exige javadoc em classes públicas -->
        <module name="JavadocType">
            <property name="scope" value="public"/>
            <property name="allowMissingJavadoc" value="true"/>
        </module>

        <!-- Proíbe espaços desnecessários -->
        <module name="WhitespaceAfter"/>
        <module name="WhitespaceAround"/>

        <!-- Proíbe imports não utilizados -->
        <module name="UnusedImports"/>

    </module>
</module>
